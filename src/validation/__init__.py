"""Real-data validation harness (planned).

This package holds the skeleton for the validation protocol described in
``VALIDATION.md``. It runs only against a credentialed MIMIC-IV / eICU-CRD
download that the user supplies via ``MIMIC_IV_ROOT``; it ships no data and no
results. Nothing here is executed by the demo app or the default pipeline.
"""
